import React from 'react';
import './Toolbar.css';
const Toolbar = (props) => (
  <header className="Toolbar">
    <div>Menu</div>
    <div>Logo</div>
    <nav>
      buttons
    </nav>
  </header>
);
export default Toolbar;
