import React from 'react';
import './Toolbar.css';
import Logo from '../../Logo/Logo';
const Toolbar = (props) => (
  <header className="Toolbar">
    <div>Menu</div>
    <Logo/>
    <nav>
      buttons
    </nav>
  </header>
);
export default Toolbar;
